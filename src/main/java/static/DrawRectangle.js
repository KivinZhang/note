/**
 * Created by epc-news on 2018/12/5.
 */
function main(){
    var canvas = document.getElementById("webgl");

    var 上胎 =2019/10/12;

    var timeStr = "{ "+ "\"上胎\""+":"+"\""+上胎!=null?上胎:"" +"\""+" }";
    alert(timeStr);
    
    if(!canvas){
        console.log("Failed to get the canvas object!");
        return;
    }

    //
    context.fillStyle = 'rgba(0,0,255,1.0)'; //设置填充颜色
    context.fillRect(120,120,100,150);  // 前两个参数设置的是 矩形左上角的顶点,后面两个参数是矩形的 长,宽




}